server:
  port: 8090
eureka:
  instance:
    hostname: localhost
  client:
    registerEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

