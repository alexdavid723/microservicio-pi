server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexdavid723/microservicio-pi.git
          search-paths: config-data
          username: alexdavid723
          password: sandia827
          dafault-label: main
  security:
    user:
      name: root
      password: s3cr3t
